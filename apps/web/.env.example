# Next.js (apps/web) — copy to .env
# Base URL of the NestJS API — the browser posts the contact form here.
# The web app never talks to Flectra; the API is the only backend it knows.
NEXT_PUBLIC_API_URL=http://localhost:4000
